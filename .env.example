# Environment Variables for AI Accountant

# LOCAL DEVELOPMENT:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your Supabase credentials below
# 3. The .env file is gitignored and won't be committed

# PRODUCTION (Vercel):
# Don't use a .env file in production
# Instead, add these variables in your Vercel project dashboard:
# Settings → Environment Variables

# Get these values from your Supabase project settings (Settings → API)
PUBLIC_SUPABASE_URL=your_supabase_url_here
PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here
